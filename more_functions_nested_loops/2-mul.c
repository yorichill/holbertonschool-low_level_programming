#include "main.h"
#include <stdio.h>

/**
 * mul -  that multiplies two integers.
 * @a: the number 1
 * @b: the number 2
 * Return: Always the multiplies of two integers.
 */

int mul(int a, int b)
{
return (a * b);
}
